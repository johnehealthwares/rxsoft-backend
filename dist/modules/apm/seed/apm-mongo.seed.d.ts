import { OnModuleInit } from '@nestjs/common';
import { Model } from 'mongoose';
import { CampaignInfoDocument, AgendaItemDocument, AchievementDocument, NewsArticleDocument, EventDocument, TestimonialDocument, MediaAssetDocument, LgaDocument, WardDocument, PollingUnitDocument, StakeholderDocument, ConversionScoreDocument, WhatsAppGroupDocument, PollingAgentDocument, ResultEntryDocument, IncidentReportDocument, GotvRecordDocument } from '../schemas';
export declare class ApmMongoSeedService implements OnModuleInit {
    private readonly campaignInfoModel;
    private readonly agendaModel;
    private readonly achievementModel;
    private readonly newsModel;
    private readonly eventModel;
    private readonly testimonialModel;
    private readonly mediaModel;
    private readonly lgaModel;
    private readonly wardModel;
    private readonly puModel;
    private readonly stakeholderModel;
    private readonly scoreModel;
    private readonly waGroupModel;
    private readonly agentModel;
    private readonly resultModel;
    private readonly incidentModel;
    private readonly gotvModel;
    private readonly logger;
    constructor(campaignInfoModel: Model<CampaignInfoDocument>, agendaModel: Model<AgendaItemDocument>, achievementModel: Model<AchievementDocument>, newsModel: Model<NewsArticleDocument>, eventModel: Model<EventDocument>, testimonialModel: Model<TestimonialDocument>, mediaModel: Model<MediaAssetDocument>, lgaModel: Model<LgaDocument>, wardModel: Model<WardDocument>, puModel: Model<PollingUnitDocument>, stakeholderModel: Model<StakeholderDocument>, scoreModel: Model<ConversionScoreDocument>, waGroupModel: Model<WhatsAppGroupDocument>, agentModel: Model<PollingAgentDocument>, resultModel: Model<ResultEntryDocument>, incidentModel: Model<IncidentReportDocument>, gotvModel: Model<GotvRecordDocument>);
    onModuleInit(): Promise<void>;
    seed(): Promise<void>;
    private seedLgas;
    private seedWards;
    private seedPollingUnits;
    private seedStakeholders;
    private seedConversionScores;
    private seedWhatsAppGroups;
    private seedCampaignInfo;
    private seedAchievements;
    private seedAgenda;
    private seedNews;
    private seedEvents;
    private seedTestimonials;
    private seedMedia;
    private seedAgents;
    private seedResults;
    private seedIncidents;
    private seedGotv;
}
