export declare class Permission {
    readonly id: string;
    readonly resource: string;
    readonly action: string;
    readonly code: string;
    constructor(id: string, resource: string, action: string, code: string);
}
