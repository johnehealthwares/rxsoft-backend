import 'reflect-metadata';
import { runSeeds } from './run';

runSeeds()
  .then(() => {
    process.exit(0);
  })
  .catch((error) => {
    console.error('Error during database seeding:', error);
    process.exit(1);
  });
